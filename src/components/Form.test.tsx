import {test, describe} from '@/test/test-utils'

describe('Form', () => {
    test.todo('input value updates on change', async () => {})
    test.todo('calls onSubmit with the input value when the form is submitted', async () => {})
    test.todo('clears the input value after form submission', async () => {})
})
