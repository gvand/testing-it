import {test, describe} from '@/test/test-utils'

describe('Counter', () => {
    test.todo('it is accessible', async () => {})
    test.todo('renders initial count value', () => {})
    test.todo('increments the count when the increment button is clicked', async () => {})
    test.todo('decrements the count when the decrement button is clicked', async () => {})
})
