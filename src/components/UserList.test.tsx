import {test, describe} from '@/test/test-utils'

describe('UserList', () => {
    test.todo('renders a list of users after fetching data', async () => {})
    test.todo('displays an error message when the data fetching fails', async () => {})
})
